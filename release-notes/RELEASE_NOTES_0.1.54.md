# 码圈输入法 0.1.54

本版把官方三码郑码的键位规则接完整，修好装入扩展字体后系统自带字反而不显示的问题，并以同一份
0.1.54 / ABI 14 核心重建 Windows、Android、Linux 和 HarmonyOS。

## 三码郑码

- 按官方方案「字顶字」：新键无法延长当前编码时，先把首候选送上屏，再用这一键起新码。用户实报打
  不出来的整句现在可以逐字打出。
- 三级简码补齐：空格、分号、句号、斜杠分别对应一级简码单字、二级简码单字和二字词，共 105 个编码
  全部可用。
- 分号候选恢复为「的、；、;」。此前生成简码时全角分号占住了「的」的完整码，最常用的字要按 2 才
  选得到；半角分号也一并补回。
- 标点会把待选的字送上屏，与官方 Rime 方案一致；句号不再堆在编码栏里，同时仍可作为三级简码键。
- 这套键位由官方码表自己声明，装上就生效，不需要任何开关，也不影响其他方案。

## 其他修复

- 修复装入扩展字体后系统自带的字反而显示不出来：字体回退不再由一张只扫了两个文件的覆盖表替系统
  下结论。「系统字体优先」只决定谁先试，不决定谁能用。
- 双拼补全不再给出比已输入音节更长的词。
- Z + 空格重复上屏改为四端默认关闭，Android 与 HarmonyOS 在形码设置中新增开关；原本依赖这个功能
  的用户需要自行开启。
- Linux 补上「两笔 30 键」开关和配套的标点顶字码长选项，与 Windows 同名同源。

## 校验

`SHA256SUMS` 与各安装包的 `.asc` 均由发布密钥 `DDEA0C1D2E89534339719927A6560A4AA577299D` 签名，
公钥见本页 `MaQuanIME-Linux-Release-Public-Key.asc`。

```bash
gpg --import MaQuanIME-Linux-Release-Public-Key.asc
gpg --verify SHA256SUMS.asc SHA256SUMS
sha256sum -c SHA256SUMS
```

HarmonyOS 请前往华为应用市场获取，本页不提供 HAP。

官网：https://maquan.app/

QQ 群：304771624
