# 码圈输入法 0.1.46

0.1.46 是 Android 键盘启动与个人词库同步的可靠性修复版。GitHub Release 提供官网直装 APK；
应用市场 AAB、HarmonyOS HAP/App Pack 和源码不公开上传。

## 修复与改进

- 修复升级、冷数据准备或同步个人词库后，输入法偶发长时间无法弹出的问题。
- 随包词库在后台原子准备并按完整文件记录进度；准备中断后从未完成项继续，失败后可以自动重试。
- 输入法会话、个人词库页面和云同步串行访问同一份个人数据，避免重复建表和数据库锁争用。
- 附加字体先判断系统字体是否缺字，仅在需要时懒加载对应字体；关闭附加字体时不额外准备字体对象。
- 保留 0.1.45 的候选区高度、按皮肤配色、击键反馈与气泡颜色设置。

## 验证

- Direct 与 MarketOffline 测试、Release lint、三 ABI 核心、渠道权限和正式签名门禁通过。
- 正式 APK 清数据冷启时，输入法服务创建约 26 ms，约 109 MB 随包数据在后台完成，窗口可先显示。
- 从空状态完成登录、恢复码和同步，个人词库恢复 14 条自造词、18 条调频，错误日志为空。
- 真实搜索框连续唤起和收起键盘 50 次：0 失败，进程稳定，无 ANR、崩溃、SQLite 锁或重复建表。

交流反馈：QQ 群 `304771624`。
