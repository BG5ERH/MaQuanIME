# 码圈输入法 0.1.45

0.1.45 是 Android 与 HarmonyOS 的移动端稳定性更新。GitHub Release 提供 Android 官网直装
APK；HarmonyOS 请从华为应用市场获取。应用市场 AAB、HarmonyOS HAP/App Pack 和源码不公开上传。

## 修复与改进

- Android 将约 109 MB 的随包数据准备移出输入法主线程，升级后可先建立输入法窗口，再在后台完成准备。
- 随包文件按原子写入后的完整文件记录进度；准备中断后从未完成项继续，成功前不把配置加载状态锁死。
- 手动同步与后台同步串行进入个人词库合并，避免首次同步同时创建 SQLite `meta` 表。
- 同步服务器上传限制提高到 8 MiB，修复约 1 MiB 词库请求被 Nginx 返回 413。
- 候选区默认高度由 17% 调整为 15%，已有用户明确保存的皮肤尺寸不被覆盖。
- 主题外观为每套皮肤分别保存候选色、击键背景色和按键气泡色；气泡选中项跟随首选候选背景色。
- 黛青默认按下色使用回车同款红色，气泡使用青色层次；其它皮肤保持原默认反馈。
- HarmonyOS 补齐附加字体、设置返回兜底与白色皮肤透明度修复。

## 验证

- Android JVM 单元测试、UTF 桥、三 ABI 一致性、Lint、渠道权限与发布签名门禁通过。
- Android 15 模拟器完成安装、输入法启用、黛青按下与长按气泡实效检查。
- HarmonyOS 逻辑测试 193 项：179 通过、14 项按测试环境条件跳过、0 失败。
- HarmonyOS 在线 Release HAP 与离线市场 App Pack 重新签名构建；0.1.45 已在 HarmonyOS 7.0
  模拟器安装、启用并切换为当前输入法。

交流反馈：QQ 群 `304771624`。
